node_modules
.output
.nitro
.tanstack
dist
count.txt
*.log
.DS_Store

# Env (server-only; valores reais NUNCA versionados — use .env.example como modelo)
.env
.env.*
!.env.example

# Playwright (E2E) — artefatos de execução
/playwright-report
/test-results
/blob-report
/playwright/.cache
