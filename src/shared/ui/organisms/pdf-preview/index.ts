/**
 * Barrel do organism `pdf-preview` — reexportado por `organisms/index.ts` → `#shared/ui`.
 * A view burra (`PdfCanvasPreview`) é a porta pública; binding/view/css são internos ao organism.
 */
export { PdfCanvasPreview, type PdfCanvasPreviewProps } from './pdf-canvas-preview.component.tsx'
