import { style } from '@vanilla-extract/css'

export const toolbar = style({
  display: 'flex',
  alignItems: 'center',
})

export const search = style({
  inlineSize: '100%',
  maxInlineSize: '28rem',
})
