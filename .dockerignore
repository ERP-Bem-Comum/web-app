# Contexto de build enxuto (handbook/reference/docker/Building best practices.md).
# Quanto menos vai ao BuildKit, mais rápido e menor a superfície de leak.

# Submódulo backend (tem build próprio em ./core-api)
core-api

# Deps e artefatos gerados
node_modules
.output
.nitro
.tanstack
dist

# Git / logs / lixo
.git
.gitignore
*.log
count.txt

# Tooling / specs / docs / testes — não entram na imagem
.claude
.specify
.planning
handbook
specs
tests
e2e
.github
.vscode
.mcp.json

# Docs markdown (preserva README se existir)
*.md
!README.md

# Ambiente e override de dev (segredos / config local nunca vão à imagem)
.env
.env.*
!.env.example
docker-compose.override.yml

# Arquivos Docker/infra (não precisam ir ao próprio contexto)
Caddyfile
docker-compose.yml
web.Dockerfile
.dockerignore
